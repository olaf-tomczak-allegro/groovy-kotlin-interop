plugins {
    id 'groovy'
    id 'org.jetbrains.kotlin.jvm' version '1.8.20'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(':kotlin18'))
    testImplementation platform('org.spockframework:spock-bom:2.4-M1-groovy-4.0')
    testImplementation group: 'org.spockframework', name: 'spock-core'
    testImplementation group: 'org.apache.groovy', name: 'groovy-all', version: '4.0.12'
}

kotlin {
    jvmToolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}

test {
    useJUnitPlatform()
}